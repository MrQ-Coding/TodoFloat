TodoFloat 1.1.4

本次更新：

- 新增桌面宠物模式：启动后默认显示 Nibble，TodoFloat 主窗口默认隐藏。
- 点击 Nibble 可以显示或隐藏 TodoFloat，待办卡片会预览当前真实任务。
- 桌面宠物保持置顶、支持拖动，并移除了原型里的底部提示条。
- 优化宠物模式启动流程，避免 TodoFloat 主窗口启动时出现黑色遮挡。

说明：

- 可以在设置菜单里关闭“桌面宠物”，关闭后 TodoFloat 会恢复普通窗口使用方式。
- 其他宠物卡片目前是功能预留，后续会再逐步接入。
